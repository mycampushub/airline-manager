{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    { "src": "/airline-management(/.*)?", "dest": "/airline-management/index.html", "headers": { "Cache-Control": "public, max-age=0, must-revalidate" } }
  ]
}